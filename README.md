# basepulse
BasePulse is a Farcaster miniapp on Base that turns user taps into real onchain activity. Each interaction randomly triggers batched smart-contract calls, emitting verifiable events and measurable usage signals across the Base ecosystem.
